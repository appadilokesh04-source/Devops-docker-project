services:
  db:
    image: mysql:8
    restart: always
    env_file:
      - .env
    volumes:
      - mysql_data:/var/lib/mysql

  web:
    build: .
    restart: always
    ports:
      - "5000:5000"
    depends_on:
      - db
    env_file:
      - .env

volumes:
  mysql_data:
